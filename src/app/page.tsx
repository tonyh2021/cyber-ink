export default function DashboardPage() {
  return (
    <main className="min-h-screen bg-surface-root flex items-center justify-center">
      <span className="text-text-muted text-sm">Dashboard — coming soon</span>
    </main>
  );
}
